# 2026-07-25

## [13:30-21:30] Claude — session กับ MK

**บริบท:** วินส่งงานที่เทรนไว้แล้วมาให้ทาง chat (13:31) — Colab notebook + Drive 2 โฟลเดอร์ (รูป+label OBB, ผลลัพธ์โมเดล) เซสชันนี้เป็นการแกะของ ทำความเข้าใจงานรุ่นพี่ แล้วออกแบบว่าจะต่อยอดยังไง **ไม่มีการเขียนโค้ดใหม่เลย**

### งานที่ทำ

**1. แกะไฟล์จากวิน (3 zip)**
- `drive-download-20260725T132121Z-1-001.zip` (9MB) → `results/` — 19 ไฟล์: args.yaml, results.csv, กราฟ, batch images
- `weights-20260725T131944Z-1-001.zip` (12MB) → `weights/` — best.pt, last.pt
- `drive-download-20260725T133335Z-1-001.zip` (**2.1GB**) → `dataset/` — images 638 / labels 559

**2. อ่านเอกสารทั้ง repo** — `README.md`, `docs/CLAUDE.md`, `docs/rule_of_tune.md`, `workmen's_diary/README.md`, `2026-07-19.md`

**3. วิเคราะห์โค้ดรุ่นพี่** (`code_augmentation+train_model+evaluate_model+test_model.py`) — 4 ขั้นในไฟล์เดียว:
- Augmentation ด้วย albumentations แบบรักษา OBB (ใช้ `keypoint_params` หมุนจุด 4 มุมตาม, `remove_invisible=False`) + เพิ่มรอบพิเศษให้คลาสหายาก (≤50% ของคลาสมากสุด)
- แบ่ง train/val 80:20 (`seed=42`)
- เทรน YOLOv8n-obb 1000 epoch / patience 50 + โค้ด resume
- Validate + อัปโหลดภาพทดสอบ (`conf=0.15`)

**4. สรุปผลโมเดลรุ่นพี่** — YOLOv8n-obb, 942 epoch (resume จาก `bridge_model-2`), imgsz 640, 7 คลาส (Crack, Efflorescence, Porous_Defect, Rust_Stained_Exposure, Spalling, Steel_Exposure, strain)
- P 91.4% / R 88.6% / mAP50 91.5% / mAP50-95 80.1%
- `best.pt` = `last.pt` ขนาดเท่ากันเป๊ะ (6,883,155 B) → optimizer state ถูก strip แล้ว → **`resume=True` ใช้ไม่ได้ แต่ fine-tune ได้**

**5. ค้นเน็ตหาโมเดลที่เหมาะกับงานระดับพิกเซล** (MK สั่ง "ไม่จำกัดแค่ YOLO งบสูงประมาณนึง")
- **Gemini 3 Pro/Flash ตัดฟีเจอร์ segmentation mask ออกแล้ว** — Google แนะให้ถอยไป 2.5 Flash → สรุป: LLM/VLM ไม่ใช่เครื่องมือนับพิกเซล
- แนะสถาปัตยกรรม 3 ชั้น: SAM 3 (text prompt แยกชิ้นส่วน, ไม่ต้องเทรน) → crack segmentation ที่ fine-tune เอง → LLM เขียนรายงานชั้นบนสุด
- เทียบ 3 ตัวเลือกชั้น segmentation: **SAC** (SAM + จูนแค่ LayerNorm 0.05%, F1 61.22/IoU 44.13 บน OmniCrack30k) / **SegFormer** (F1 59.98/IoU 42.85 เบนช์เดียวกัน, 3.7M) / **AGMamba** (F1 76.22 แต่บน **Crack500** = เบนช์ง่ายกว่า เทียบตรงไม่ได้ + หา repo ไม่เจอ)
- ค่า GPU ทั้ง 3 ตัวรวมกันไม่ถึง 2,000฿ → ไม่ใช่ตัวแปรตัดสินใจ; ต้นทุนจริงคือค่า label + เวลาคน
- แนะเริ่ม SegFormer-B0 บน Colab ฟรีก่อน ค่อยขยับไป SAC ถ้า IoU ไม่พอ

**6. ตรวจ license OmniCrack30k** (30k ภาพ ~9 พันล้านพิกเซล) — **GPL-3.0 + non-commercial only, โหลดตรงไม่ได้ ต้องส่งเมลขอจากอีเมลมหาวิทยาลัย**, มีแค่คลาส crack (ไม่มี spalling/steel exposure) → ใช้ได้แค่ pretrain คลาส crack

**7. ออกแบบวิธีแยกระดับความรุนแรง 1-4** (MK ย้ำว่าไม่ใช่การระบุประเภท)
- ข้อสรุป: **อย่าเทรน classifier ทาย 1-4 — ให้คำนวณจากค่าที่วัดได้** เหตุผล: scale ambiguity (รอยแตก 0.3mm ระยะ 2m = 3mm ระยะ 20m ในพิกเซล), label noise, อธิบายไม่ได้ตอน defend, CS4 หาภาพยาก, ต้อง label 638 ภาพใหม่หมด
- ประเภทความเสียหายไม่หายไป — เปลี่ยนบทบาทเป็น *input* ของสูตร (เหล็กโผล่ override เป็น CS สูงทันที)
- MK ชี้ปัญหาว่านับพิกเซลหาขนาด mm ไม่ได้ → **ทางออก: หารด้วยความกว้างชิ้นส่วนในภาพเดียวกัน** (ความกว้างรอยแตก px ÷ ความกว้างเสา px = ไร้หน่วย สเกลหักล้างกัน) ถ้าภายหลังรู้ขนาดเสาจริงจากทะเบียนสะพาน ค่าเดียวปลดล็อก mm ย้อนหลังทุกภาพของสะพานนั้น
- ตัวชี้วัดที่ไม่ต้องรู้สเกล 5 ตัว: extent %, relative width, crack density, pattern/ทิศทาง (ได้ฟรีจาก OBB angle), type hierarchy
- แบ่งบทบาทข้อมูล: 638 ภาพรุ่นพี่ (ไม่รู้ระยะถ่าย) → เทรน segmentation เท่านั้น / ภาพที่ทีมถ่ายเอง → ใช้ประเมิน severity
- สูตรต้องคืน "เหตุผล + ข้ออ้างอิง" ไม่คืนเลขเปล่า; `width_mm` คืน `None` เมื่อไม่รู้ขนาดเสา (ไม่เดา ตามกฎข้อ 3 rule_of_tune)

### ไฟล์ที่เปลี่ยน

- `rawdata/งานของรุ่นพี่/results/` (ใหม่) — แกะจาก zip
- `rawdata/งานของรุ่นพี่/weights/` (ใหม่) — แกะจาก zip
- `rawdata/งานของรุ่นพี่/dataset/` (ใหม่) — แกะจาก zip, 1,197 ไฟล์ ~2.0GB
- MK commit เองที่ `97c4f5f " data from รุ่นพี่"` (20:49, 1,220 ไฟล์) แล้ว `e9fbe6f "ลบข้อมูล"` (21:22, ลบ zip 2.1GB ออกจาก worktree)
- Claude ไม่ได้แก้/สร้างไฟล์อื่นเลยนอกจากไดอารี่นี้

### ⚠️ ผลกระทบ/ข้อควรระวัง

- **🔴 ด่วน: push จะพัง — zip 2.1GB ติดอยู่ใน git history แล้ว** commit `97c4f5f` เพิ่มไฟล์ zip 2,126,058,894 B เข้า git; `e9fbe6f` ลบออกจาก worktree แต่ **blob ยังอยู่ใน history** GitHub จำกัดไฟล์ละ 100MB → `git push` (ค้าง 2 commit) **จะถูกปฏิเสธแน่นอน**
  - `.git` ตอนนี้ **3.9GB**
  - ต้องแก้ด้วยการ rewrite history (`git filter-repo` / BFG) เอา blob zip ออกก่อน push — **อย่า push ก่อนแก้** และเป็นงาน destructive ต้องได้ MK ยืนยัน
  - พิจารณาว่า dataset 2GB ควรอยู่ใน git ไหม (Git LFS หรือ .gitignore + เก็บใน Drive)
- **images 638 vs labels 559 ไม่เท่ากัน (ต่าง 79 ไฟล์)** — ยังไม่ตรวจว่าเป็น background image ที่ไม่มี label โดยตั้งใจ หรือ label หายตอน export จาก Roboflow (ชื่อไฟล์รูปแบบ `*_jpg.rf.<hash>` = Roboflow export)
- โค้ดรุ่นพี่ `A.Resize(640,640)` — ถ้าใช้กับภาพ UAV ความละเอียดสูง **รอยแตกบางจะหายก่อนโมเดลได้เห็น** ต้องเปลี่ยนไปใช้ tiling/sliding window (SAHI, overlap ~20%)
- เกณฑ์ CS ในโค้ดที่ร่างคุยกันเป็น **placeholder ทั้งหมด** ยังไม่มีตัวเลขจริง
- zip ต้นฉบับ 2 ไฟล์ (results/weights) ยังอยู่ในโฟลเดอร์ ไม่ได้ลบ (ตามกฎ rule_of_tune ต้องขออนุญาตก่อน)

### ค้างไว้ / ทำต่อ

1. **แก้ git history เอา zip 2.1GB ออก ก่อน push** (blocker)
2. **ยังไม่ได้อ่าน `1_Foundation/0_[อ่านก่อน] เกณฑ์ประเมิน T-BHI`** — เป็น unknown ที่สำคัญสุด: **ถ้า T-BHI ตัดเกณฑ์ด้วย % extent ไม่ใช่ความกว้าง mm ปัญหาสเกลทั้งหมดหายไปเลย** ต้องอ่านก่อนออกแบบต่อ
3. อ่าน `FHWA Element Condition State (handout).pdf` → ทำตาราง CS 1-4 × 7 คลาส + ช่อง "วัดจากอะไร / AI ทำได้ไหม"
4. อ่าน `3_Gap+Method/Celik - Pixels to Rating` — ชื่อตรงโจทย์เป๊ะ อาจแก้ปัญหานี้ไปแล้ว
5. ตรวจ 79 ไฟล์ที่ image/label ไม่ตรงกัน
6. ส่งเมลขอ OmniCrack30k จาก `@ku.th` (รออนุมัติ ไม่ใช่โหลดทันที) — หรือใช้ Crack500/DeepCrack ที่โหลดได้เลย
7. mini-project (จำแนกความรุนแรงเสา) ยังไม่เริ่มลงมือ — มีแค่แผน
8. เปเปอร์ที่ยังไม่อ่าน: T-BHI, BMMS, NBI/AASHTO, CNN review, Chen 2023, Lateef&Yu, Gou 2026, Yasuno 2026 (พักไว้ตามคำสั่ง MK ในเซสชัน 2026-07-19)
9. README/CLAUDE.md ยังไม่มีชื่อโปรเจกต์เต็ม/ทีม/timeline จริง (ค้างจาก 2026-07-19)

---
