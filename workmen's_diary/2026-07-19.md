## [ช่วงบ่าย-เย็น] Claude — session กับมะขาม

**งานที่ทำ:**
- ตั้งไฟล์มาตรฐานของโปรเจกต์ให้ครบชุด (เทียบเคียงกับที่ repo `Constistant` มีอยู่แล้ว): `CLAUDE.md`, `.gitignore`, `.claude/settings.json`, และแก้ `README.md` จาก placeholder (`"123"`) ให้เป็นคำอธิบายโปรเจกต์จริง
- ลบไฟล์ `Chapter 07A Geometric Design rev20200703.pdf` (5.4MB) ที่ลอยอยู่ root — ไม่เข้าพวกกับเนื้อหาเรื่องตรวจสอบสภาพสะพาน/เสา ตามที่ผู้ใช้สั่งให้ลบ
- สร้างระบบ `workmen's_diary/` และ `rule_of_tune.md` (ปรับ context จาก repo `Training` ให้เหมาะกับโปรเจกต์นี้)

**ไฟล์ที่เปลี่ยน:**
- `CLAUDE.md` (ใหม่ → ย้ายเป็น `docs/CLAUDE.md`) — สรุปโครงสร้างโปรเจกต์ (KU senior project เรื่องตรวจสอบสภาพสะพาน/เสาด้วย AI ต่อยอดจากงานวิจัย วช.), แผนผัง `rawdata/`, เตือนไม่ให้อ่าน PDF ทั้งหมดมั่วๆ
- `.gitignore` (ใหม่) — ก่อนหน้านี้ไม่มีเลย ทั้งที่ repo commit PDF ไปแล้ว ~121MB โดยไม่มีการกันไฟล์ junk เลย
- `.claude/settings.json` (ใหม่) — permission allowlist พื้นฐาน
- `README.md` — เปลี่ยนจาก `"123"` เป็นคำอธิบายโปรเจกต์ + โครงสร้างโฟลเดอร์ + ลิงก์ไป `docs/CLAUDE.md` + เตือนว่า CLAUDE.md ไม่ auto-load แล้ว
- `Chapter 07A Geometric Design rev20200703.pdf` — ลบ (git rm) ตามคำสั่งผู้ใช้
- `workmen's_diary/README.md` (ใหม่) — กติกาการเขียนไดอารี่ ปรับจาก Training ให้เป็น single-repo (**ปล่อยไว้ที่ root ตามเดิม** — ผู้ใช้ยืนยันไม่ต้องย้าย เพราะจัดกลุ่มเป็นระเบียบอยู่แล้ว)
- `rule_of_tune.md` (ใหม่ → ย้ายเป็น `docs/rule_of_tune.md`) — กฎการแตะต้อง `rawdata/` (ไฟล์อ้างอิงงานวิจัยต้นฉบับ) ปรับ context จาก `Training/training-data/docs/rule_of_tune.md` (ซึ่งคุ้มครอง raw JSON fine-tuning data) มาเป็นคุ้มครองไฟล์วรรณกรรม/รายงานต้นฉบับของโปรเจกต์นี้แทน
- **`docs/`** (ใหม่) — ย้าย `CLAUDE.md` และ `rule_of_tune.md` เข้ามารวมกันตามคำขอผู้ใช้ ("ไฟล์ md ยกเว้น readme รวมไว้ที่เดียว") root เหลือแค่ `README.md` (+ `workmen's_diary/` ซึ่งเป็นโฟลเดอร์จัดระเบียบแล้ว ไม่ใช่ไฟล์ loose)
- `README.md` — ขยายเนื้อหาเต็มรูปแบบตามคำขอ "ทำไฟล์ readme ให้มีเนื้อหาจำเป็น และดีๆ": เพิ่มสถานะโปรเจกต์ (literature review เสร็จ/methodology+reports ยังไม่เริ่ม), โครงสร้าง repo แบบ tree เต็ม, section สำหรับ Claude session, หมายเหตุเรื่อง repo (ขนาด rawdata, ไม่มี CI)

**ผลกระทบ/ข้อควรระวัง:**
- `rawdata/` (~121MB) ยังอยู่ใน git history เหมือนเดิม การเพิ่ม `.gitignore` ตอนนี้ป้องกันได้แค่ของใหม่ในอนาคต ไม่ย้อนกลับไปแก้ของเก่า (ยังไม่ได้ตัดสินใจว่าจะทำอะไรกับเรื่องนี้)
- **`docs/CLAUDE.md` จะไม่ auto-load อีกต่อไป** เพราะ Claude Code หา `CLAUDE.md` เฉพาะที่ root/โฟลเดอร์แม่เท่านั้น — ผู้ใช้เลือกเองแบบรู้ตัวว่ายอม sacrifice จุดนี้เพื่อความเป็นระเบียบ ต้องบอก Claude ให้อ่าน `docs/CLAUDE.md` เองทุกครั้งที่เริ่ม session ใหม่ (มีเตือนไว้ใน README.md แล้ว)
- ยังไม่ commit การเปลี่ยนแปลงทั้งหมดนี้เข้า git — รอผู้ใช้ยืนยัน

**ค้างไว้ / ทำต่อ:**
- ผู้ใช้ยังไม่ได้กรอกชื่อโปรเจกต์เต็ม/ทีม/timeline ที่จริงลงใน `CLAUDE.md`/`README.md` (ตอนนี้เป็นข้อมูลที่ Claude อนุมานจากชื่อไฟล์/โฟลเดอร์เท่านั้น)
- `rule_of_tune.md` ยังไม่มี "บทเรียนจริง" — ปล่อยว่างไว้ตามหลัก ห้ามใส่บทเรียนสมมติ จะเพิ่มเมื่อมีเหตุการณ์จริงเกิดขึ้น
- ยังไม่ commit/push อะไรเลย

---

## [ช่วงบ่าย-เย็น] Claude — session กับ MK (แยกจากเซสชันข้างบน)

**บริบท:** MK ให้ clone repo `ku_project_jop` จาก GitHub มาทำงาน (ตอนนั้น repo ว่างเปล่า) ย้ายมาไว้ที่ `D:\00mk\เสาพัง\ku_project_jop` แล้วอัป `rawdata/` ขึ้นไป จากนั้นอ่านเอกสาร literature review 2 ไฟล์ตามไกด์ ก่อน MK สั่งจำกัด scope งานจริงให้แคบลง

**งานที่ทำ:**
- Clone + ย้าย repo, อัป `rawdata/` (18 ไฟล์ ~121MB) — เจอปัญหา Windows path ยาวเกิน → เปิด `core.longpaths` (local, ตามที่ MK ยืนยัน)
- แก้ push ค้าง (403 permission denied): บัญชี GitHub บนเครื่องชนกัน (`whysaomaitrongbaab-collab` ที่ใช้กับ Constistant/Training vs `chaichanaju-dot` เจ้าของ repo นี้) — ลบ credential cache เก่าด้วย Win32 `CredDelete` API (cmdkey ธรรมดาลบไม่ได้เพราะ URL มี `:` ทำให้ parser พัง) → ตั้ง SSH key แยกเฉพาะบัญชีนี้ (`~/.ssh/id_ed25519_chaichanajudot` + host alias `github-chaichanajudot` ใน `~/.ssh/config`) → merge `--allow-unrelated-histories` กับไฟล์ที่มีอยู่แล้วบน remote (README.md, Chapter 07A PDF) → push สำเร็จ
- อ่าน literature review: วช.68 (Tier 2 เต็ม: S-BCRI คัดกรอง 9,629 สะพาน + UAV/LiDAR/SLAM Digital Twin + YOLOv10 mAP@0.5 46.7% + 3D-FEM) และ 203499 (รุ่นพี่: point cloud 212M จุด → orthographic image → **Gemini 3 Pro VLM ผ่าน Roboflow ตรวจจับความเสียหาย 4 คลาส แบบ zero-shot**) — สรุปให้ MK ว่า gap ของ Tier 1 คือ "เร็ว/ข้าม 3D pipeline" ไม่ใช่การเป็นเจ้าแรกที่ใช้ VLM (203499 ใช้ไปแล้ว)
- MK ตัดขอบเขตงานจริง: mini-project เทรน/tune AI จำแนก**ความรุนแรงความเสียหายเสา**จากภาพ ไม่ทำ 3D/FEM/S-BCRI เต็ม pipeline — หยุดอ่านเปเปอร์ที่เหลือ (T-BHI, BMMS, NBI/AASHTO, เปเปอร์ AI Landscape/Gap+Method อีก 7 ไฟล์)
- เช็คปลั๊กอิน "ponytail" (Claude Code marketplace) ตามคำขอ MK — repo อายุ 5 สัปดาห์แต่ 85.8k ดาว/subscriber แค่ 198 คน (สัญญาณดาวปลอม) เช็คโค้ด hook จริงแล้วไม่มีอะไรอันตราย แต่แนะนำไม่ติดตั้งเพราะตัวเลขโฆษณาเชื่อไม่ได้และไม่ตรง use-case เทรนโมเดล
- ทำ Artifact โฟลว์ชาร์ต 9 ขั้นตอนของ mini-project (scope→data→label→split→train/eval-branch→train→evaluate→verify→wrap-up) พร้อม font Sarabun+IBM Plex Mono embed เอง

**ไฟล์ที่เปลี่ยน (ผ่าน git):**
- `rawdata/` — เพิ่มทั้งโฟลเดอร์ (commit `2047528`)
- merge commit `2e7d2d4` — รวมกับ README.md/Chapter 07A PDF ที่มีอยู่แล้วบน remote
- `.git/config` (local) — remote URL เปลี่ยนเป็น SSH ผ่าน host alias `github-chaichanajudot`

**⚠️ พบระหว่างเซสชัน — สำคัญ ต้องแจ้ง MK:**
- ตอนจะเขียนไดอารี่นี้ พบว่ามี**คนอื่นทำงานคู่ขนานในรีโปเดียวกันแล้ว**: commit `ec821b8 "สรุป"` เป็นของ **`nutta-sit <nuttasit.th@ku.th>`** (ไม่ใช่ MK) เพิ่งเพิ่มไฟล์ `สรุป_01_Literature_Review.docx` เข้ามา
- นอกจากนี้ working directory มีการเปลี่ยนแปลง**ที่ยังไม่ได้ commit**: ลบ `Chapter 07A Geometric Design rev20200703.pdf` (staged), แก้ `README.md` (unstaged), และไฟล์ใหม่ `.claude/`, `.gitignore`, `docs/` (untracked) — มาจากเซสชันด้านบนในไฟล์นี้เอง (อ้างว่าเป็น "session กับมะขาม" แต่ commit จริงเป็นชื่อ nutta-sit เลยไม่แน่ใจว่าเป็นคนเดียวกับ MK หรือทีมงานคนละคน)
- **ยังไม่ได้ commit/push อะไรจากเซสชันนี้ (Claude session กับ MK) เข้าไปทับ** — ปล่อยของค้างไว้ตามที่เจอ ไม่แตะ ให้ MK ตรวจสอบ/คุยกับ nutta-sit ก่อนว่าจะ commit อะไรบ้าง

**ค้างไว้ / ทำต่อ:**
- MK ต้องตรวจสอบ/ประสานกับ nutta-sit เรื่องการเปลี่ยนแปลงที่ค้างอยู่ (docs/, .gitignore, .claude/, ลบ Chapter 07A PDF) ก่อน commit
- mini-project (จำแนกความรุนแรงเสา) ยังไม่เริ่มลงมือจริง มีแค่แผนงาน (ดู Artifact โฟลว์ชาร์ต)
- ยังไม่ได้อ่านเปเปอร์ที่เหลือ (T-BHI, BMMS, NBI/AASHTO, CNN review, Chen 2023, Lateef&Yu, Gou 2026, Yasuno 2026, Çelik) — พักไว้ตามคำสั่ง MK

---
